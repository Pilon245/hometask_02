// import {OutputBlogsDbType} from "./types/blogsTypes";
//
// type sortBy = {
//     searchNameTerm: string
//     direction: "asc" | "desc"
// }
//
// export const getName = <T>(it) => {
//     return [...items].sort((u1,u2) => {
//         for(let sortConfig of sortBy){
//             if(u1[sortConfig.searchNameTerm] < u2[sortConfig.searchNameTerm]){
//                 return sortConfig.direction === 'asc' ? -1 : 1
//             }
//             if(u1[sortConfig.searchNameTerm] > u2[sortConfig.searchNameTerm]){
//                 return sortConfig.direction === 'asc' ? 1 : -1
//             }
//         }
//         return 0
//         }
//     )
// }
// type sortCreated = {
//     createdAt: string
//     direction: "asc" | "desc"
// }
